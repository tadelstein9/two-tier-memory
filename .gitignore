# tier-2 store and generated tier-1 index are the user's own data
memory.db
INDEX.md
# per-session loaders reconstruct private rows (paths, business specifics) -- data, not tool
load_*.sh
__pycache__/
*.pyc
